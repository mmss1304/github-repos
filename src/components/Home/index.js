import React from "react";

import Header from "../Header";
import Repos from "../Repos";

const Home = () => {
  return (
    <React.Fragment>
      <Header />
      <Repos />
    </React.Fragment>
  );
};

export default Home;
